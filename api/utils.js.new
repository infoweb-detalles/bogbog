const { storeAndBroadcastEvent } = require('./events');

// Función para transmitir eventos
function broadcastEvent(event) {
    try {
        storeAndBroadcastEvent(event);
    } catch (error) {
        console.error('Error al transmitir evento:', error);
    }
}

module.exports = {
    broadcastEvent
};